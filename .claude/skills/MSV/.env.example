# MSV Skill Environment Variables
# Copy this file to .env and fill in your values
# Or set these as system environment variables

# =============================================================================
# API Keys (Optional - MSV works without these, but with reduced data)
# =============================================================================

# VulnCheck API Key (Recommended)
# Get free API key at: https://vulncheck.com
# Provides: Exploit/PoC intelligence, enhanced KEV data
# VULNCHECK_API_KEY=vulncheck_your_key_here

# NVD API Key (Recommended for Batch Operations)
# Get FREE API key at: https://nvd.nist.gov/developers/request-an-api-key
# Provides: 10x faster rate limits (50 req/30s vs 5 req/30s without key)
# Without key: Batch of 100 products takes ~10 minutes
# With key:    Batch of 100 products takes ~1 minute
# NVD_API_KEY=your_nvd_key_here

# =============================================================================
# Configuration (Optional)
# =============================================================================

# Custom cache directory
# Default: MSV skill's data/ folder
# MSV_CACHE_DIR=/path/to/custom/cache

# Cache duration in hours (default: 4 for KEV, 24 for others)
# MSV_CACHE_HOURS=24

# =============================================================================
# Debug Options (Development Only)
# =============================================================================

# Enable verbose logging
# MSV_DEBUG=true

# Skip specific data sources (comma-separated)
# MSV_SKIP_SOURCES=vulncheck,nvd
