# FoldingAtCloud Environment Variables
# Copy to $PAI_DIR/.claude/.env and fill in values

# =============================================================================
# Folding@Home Account
# =============================================================================
FAH_USERNAME=YourUsername
FAH_TEAM_ID=245143
FAH_PASSKEY=your_passkey_here
FAH_EMAIL=your_email@example.com
FAH_ACCOUNT_TOKEN=your_account_token_here
FAH_PORTAL_URL=https://v8-4.foldingathome.org/
FAH_MACHINE_PREFIX=pai-fold

# =============================================================================
# SSH Credentials - Provider Specific
# =============================================================================
# IMPORTANT: Each provider may use different SSH keys and usernames

# Azure
AZURE_SSH_USER=foldingadmin
AZURE_SSH_KEY=$HOME/.ssh/azure_hashcrack

# OCI (Oracle Cloud)
OCI_SSH_USER=ubuntu
OCI_SSH_KEY=$HOME/.ssh/id_ed25519

# AWS
AWS_SSH_USER=ubuntu
AWS_SSH_KEY=$HOME/.ssh/aws_hashcrack

# GCP
GCP_SSH_USER=foldingadmin
GCP_SSH_KEY=$HOME/.ssh/gcp_hashcrack

# =============================================================================
# Budget Limits (USD)
# =============================================================================
FOLDING_BUDGET_DAILY=5.00
FOLDING_BUDGET_MONTHLY=50.00

# =============================================================================
# Graceful Shutdown
# =============================================================================
# Maximum time (seconds) to wait for work unit completion before timeout
FOLDING_GRACEFUL_TIMEOUT=1800

# =============================================================================
# OCI Configuration
# =============================================================================
# OCI uses ~/.oci/config for API authentication
# Ensure these are set in ~/.oci/config:
#   user, fingerprint, tenancy, region, key_file
#
# Note: OCI CLI (oci.exe) may be slow. Prefer terraform for OCI operations.
