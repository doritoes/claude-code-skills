# =============================================================================
# Hashcrack Skill - Environment Variables
# =============================================================================
# Copy this file to .claude/.env (your project root) and fill in your values.
# These values are shared across all terraform deployments.
# =============================================================================

# =============================================================================
# Hashtopolis Server (after deployment)
# =============================================================================
# These are set automatically after running a deployment workflow.
# You can also set them manually if you have an existing server.

HASHCRACK_SERVER_URL=http://YOUR_SERVER_IP:8080
HASHCRACK_ADMIN_USER=hashcrack
HASHCRACK_ADMIN_PASSWORD=CHANGE_ME
HASHCRACK_API_KEY=YOUR_API_KEY
HASHCRACK_VOUCHER=YOUR_VOUCHER

# =============================================================================
# AWS Credentials (for AWS deployments)
# =============================================================================
# Get these from AWS Console -> IAM -> Users -> Security credentials

AWS_ACCESS_KEY_ID=AKIA...
AWS_SECRET_ACCESS_KEY=...
AWS_DEFAULT_REGION=us-east-1

# =============================================================================
# Azure Credentials (for Azure deployments)
# =============================================================================
# Authentication via 'az login' is recommended for local development.
# For automation, create a Service Principal:
# az ad sp create-for-rbac --name "hashcrack-sp" --role="Contributor"

# AZURE_SUBSCRIPTION_ID=...
# AZURE_TENANT_ID=...
# AZURE_CLIENT_ID=...
# AZURE_CLIENT_SECRET=...

# =============================================================================
# GCP Credentials (for GCP deployments)
# =============================================================================
# Authentication via 'gcloud auth application-default login' is recommended.
# Or set GOOGLE_APPLICATION_CREDENTIALS to a service account JSON file.

# GOOGLE_PROJECT=your-project-id
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account.json

# =============================================================================
# OCI Credentials (for Oracle Cloud deployments)
# =============================================================================
# Get these from OCI Console -> User Settings -> API Keys

# OCI_TENANCY_OCID=ocid1.tenancy.oc1..aaaa...
# OCI_USER_OCID=ocid1.user.oc1..aaaa...
# OCI_FINGERPRINT=xx:xx:xx:xx:xx:xx:xx:xx:xx:xx:xx:xx:xx:xx:xx:xx
# OCI_PRIVATE_KEY_PATH=~/.oci/oci_api_key.pem

# =============================================================================
# Proxmox Credentials (for local Proxmox deployments)
# =============================================================================

# PVE_HOST=192.168.1.100
# PVE_USER=root@pam
# PVE_PASSWORD=CHANGE_ME
# PVE_INSECURE=true

# =============================================================================
# XCP-ng / Xen Orchestra (for XCP-ng deployments)
# =============================================================================

# XO_HOST=192.168.1.200
# XO_USER=admin
# XO_PASSWORD=CHANGE_ME
